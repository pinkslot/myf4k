// Main executable for the detection and tracking of fish from an input video.
// Common includes
#include <iostream>
#include <log.h>

using namespace std;

int main(int argc, char** argv)
{
	Log::setInfoLogStream(cout);
	Log::info(0) << "here would be training of the classifier" << endl;
}