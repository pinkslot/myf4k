# External executables

# Where is the project root folder?
ROOT_PREFIX := ../../../
# Includes and libraries
LIBS :=  -leval -lgabor -lmotion_vector -lxmltd -lpr -ldet_summary -lalgorithms -lnewdb -lutils -lcvblob -llog -lmysqlcppconn -lticppd -lsvm -lboost_thread
APPEND_OPENCV_LIBS := 1

# Define subprojects
SUB_PROJS := 

# Simple executables (i.e. having only the cpp file as a dependency)
EXE_TARGETS := prova

# Library
LIB_SHORT_NAME := svm_det_eval

# Precompiled objects
PRECOMPILED_OBJECTS :=

# Include main makefile
include $(ROOT_PREFIX)subproj_makefile.mk
